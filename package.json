{
  "name": "map-to-constellation",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first map drawing with spacing-preserving PNG and SVG export.",
  "scripts": {
    "start": "node server.cjs",
    "test": "node --test tests/*.test.cjs",
    "build": "node build.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "maplibre-gl": "6.9.0"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
